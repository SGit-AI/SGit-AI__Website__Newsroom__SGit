# The editor's inbox

Paste what "Copy for Claude" gives you in the reading room into a new file here, `YYYY-MM-DD__feedback__<device>.md`,
and commit it (or paste it into a session and ask the Editor desk to file it). Run reports from `/newsroom-run` land
here too. The Editor reads every file on its next run and passes each item to the desk it concerns.
